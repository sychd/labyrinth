"use strict";
describe("new Field()", function () {
  it("Запонение массива эл-ов", function () {
    assert.isNotNull(cells);
  });
  it("Заполение массива пути", function () {
    assert.isNotNull(way);
  });

  it("Отрисовка лабиринта", function () {
    for (let i = 0; i < 0; i++) {
      assert.isNotNull(cells[i][0].divElem);
    }
  });
});
describe("new PathFinder()", function () {
  it("Найден ли выход", function () {
    assert.equal(way[way[way.length - 1], finish]);
  });
});

